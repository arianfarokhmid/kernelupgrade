**or just for updating from apt package  manager do this:**

```bash
sudo apt install linux-image-generic
```

```bash
apt list --upgradable | grep linux-image
```

```bash
apt-cache search linux-image
```
